# CODEXA C1 M1 Resume

Date: 2026-09-09
Authority: `CODEXA_C1_MASTER_PLAN_v10`
Track A target: `C1-66 = 65,690,496` parameters

## State Found

- `config.py` and `parameter_calculator.py` report the required configuration: 30 layers, hidden size 384, 6 query heads, 3 KV heads, head dimension 64, FFN size 1152, and vocabulary size 32768.
- The calculated parameter count is exactly `65,690,496`.
- `CODEXA_SESSION.md` identifies C1-66 and marks 604M as forbidden as a training target.
- `data/source_registry.jsonl` contains approved `fineweb2_arz_arab` and has no NileChat or Fineweb-edu-Egypt entry.
- Registry validation passed: all five JSONL records parsed successfully.
- Local parquet data exists under `data/raw/fineweb2_arz_arab/`, including an approximately 1.14GB train shard and a small test shard. An existing arb sample is present and was not replaced.
- The pre-existing `data/tokenizer/corpus.txt` is only 599 bytes and was not treated as the new corpus.

## Work Completed

- `data_pipeline/ingest_hf_to_parquet.py` remains torch-free, registry-gated, and limited to the approved FineWeb source/config identities. It now accepts `--source-id` and validates `--sample-one` for the Arabic sample path.
- `data_pipeline/build_tokenizer_corpus.py` remains DuckDB-based, uses `text.normalize.normalize_text`, applies SHA-256 deduplication, excludes `data/tokenizer_eval`, and enforces the 40..8000 character range. English target is set to zero for the weak local device.
- Both data pipeline scripts pass `py_compile`.

## Incomplete Work

The local corpus build was started against the large existing parquet files, but did not produce a complete v10 result during this session. Current observed state was:

- `data/tokenizer_train_v10/ar.txt`: approximately 2.65GB
- `data/tokenizer_train_v10/arz.txt`: missing
- `data/tokenizer_train_v10/en.txt`: missing by design (`TARGET_EN=0`)
- `data/manifests/tokenizer_train_v10.json`: missing
- `data/tokenizer/corpus.txt`: still 599 bytes

The existing tokenizer artifact remains unchanged by this M1 continuation and was not staged. No network download, model training, or changes to `model.py` or `train.py` were performed.

## Decision

`retrain-need-more-data`: do not freeze T0. The v10 corpus and manifest are incomplete, so tokenizer retraining must wait until the local corpus builder completes successfully and produces a corpus materially larger than the historical approximately 10KB body.